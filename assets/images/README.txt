Put real image assets here.

Priority:
1. Headshot of Jack — e.g. jack.jpg (square-ish, min ~600x750px, good light).
   Then in index.html, swap the <div class="avatar"> in the About section for:
     <img class="avatar" src="assets/images/jack.jpg" alt="Jack Burdett" />
   (add object-fit:cover to .avatar if needed).

2. Optional: GBSC logo / a real screenshot of the live club assistant, if the club is happy to be shown.

Keep files web-optimised (compress; aim < 300KB each).
